namespace AvukatProLib.Util
{
    public class VeriTabaniKullancilariMetotlar
    {
        //public static List<string> MevcutVeriTabani(string constr)
        //{
        //    //ServerConnection srvcon = new ServerConnection();
        //    Server server = new Server();//srvcon);
        //    //string[] sonuc = null;
        //    int i = 1;
        //    List<string> sonuc = new List<string>();

        //    foreach (Database dbServer in server.Databases)
        //    {
        //        sonuc.Add(dbServer.Name);

        //    }
        //    return sonuc;
        //}
        //public DataTable KullaniciBilgisi(string srvcon)
        //{
        //    Server server = new Server();//srvcon);
        //    List<string> sonuc = new List<string>();
        //    List<string> kullanýcýlar = new List<string>();

        //    List<string> langurage = new List<string>();
        //    DataTable dt = new DataTable();

        //    foreach (Database dbServer in server.Databases)
        //    {
        //        sonuc.Add(dbServer.Name);
        //        kullanýcýlar.Add(dbServer.Users[dbServer.UserName].Login);
        //        //SFR.Add(dbServer.Collation);
        //        langurage.Add(dbServer.Collation);

        //    }
        //    dt.Columns.Add("Database");
        //    dt.Columns.Add("Users");
        //    dt.Columns.Add("langurange");

        //    DataRow dr = null;
        //    for (int i = 1; i < sonuc.Count; i++)
        //    {
        //        dr = dt.NewRow();
        //        dr["Database"] = sonuc[i].ToString();
        //        dr["Users"] = kullanýcýlar[i].ToString();
        //        dr["langurange"] = langurage[i].ToString();
        //        dt.Rows.Add(dr);
        //    }

        //    return dt;
        //}
    }
}